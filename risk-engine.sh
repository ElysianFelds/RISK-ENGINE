#!/bin/bash
# Launches the interactive Signal Engine menu.
# Double-click this (Mac: right-click > Open, or run ./risk-engine.sh)
# or type ./risk-engine.sh from inside this folder.
cd "$(dirname "$0")"
python3 menu.py
